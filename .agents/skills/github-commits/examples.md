# Server commit examples

**feat**
```
feat(workspace): expire invite links after max_uses
```

**fix**
```
fix(permissions): grant workspace owner edit without board_members row
```

**db**
```
chore(db): add index on board_operations(timestamp)
```

**socket**
```
fix(socket): drop empty offline rooms on last disconnect
```
